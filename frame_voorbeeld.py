from Tkinter import *

root = Tk()
frame = Frame(root,height=500,relief=GROOVE)
frame.pack(fill=X, padx=5, pady=5)

#bottomframe = Frame(root)
#bottomframe.pack( side = BOTTOM )

#leftframe=Frame(root,relief=GROOVE,height=500)
#leftframe.pack(side=LEFT,expand=YES)

#rightframe= Frame(root,relief=GROOVE)
#rightframe.pack(side=RIGHT)

#topframe= Frame(root,relief=GROOVE)
#topframe.pack(side=TOP)

#redbutton = Button(leftframe, text="Red", fg="red")
#redbutton1=Button(leftframe, text="Red1", fg="red")
#redbutton.pack( side = LEFT)
#redbutton1.pack( side = LEFT)

#greenbutton = Button(frame, text="Brown", fg="brown")
#greenbutton.pack( side = LEFT )

#bluebutton = Button(frame, text="Blue", fg="blue")
#bluebutton.pack( side = LEFT )

#blackbutton = Button(bottomframe, text="Black", fg="black")
#blackbutton.pack( side = BOTTOM)

root.mainloop()
