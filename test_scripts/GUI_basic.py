from Tkinter import *

class App:
    
    def__init__(self):

        frame=Frame(width=1000, height=1000, bg="", colormap="new")
        frame.pack()

        
#video.attach_window(frame.window_id())

        root=Tk()
        app=App(root)
        root.mainloop()
